export type IdentityFile = {
    uri: string;
    name: string;
    type: string;
};

export type HostessForm = {
  firstName: string;
  lastName: string;
  phone: string;
  dateOfBirth: string;
  identityNumber: string;
  email: string;
  password: string;
  username: string;
};